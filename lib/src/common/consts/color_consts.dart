import 'package:flutter/material.dart';

abstract class ColorConsts {
  static const favoriteIconChosen = Colors.amber;
  static const favoriteIconHollow = Colors.grey;
  static const deadStatusColor = Color.fromARGB(255, 116, 89, 87);
  static const aliveStatusColor = Colors.green;
  static const unknownStatusColor = Colors.grey;
}
